{
  "name": "aniluna",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bash start.sh",
    "build": "pnpm --filter @workspace/api-server run build && PORT=5000 pnpm --filter @workspace/animestream run build",
    "start": "bash start-prod.sh",
    "db:push": "pnpm --filter @workspace/db run push"
  },
  "packageManager": "pnpm@9.15.4"
}
